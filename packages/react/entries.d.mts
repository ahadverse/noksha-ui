/**
 * Types for `entries.mjs`, which is plain JS because the build itself imports
 * it — `tsup.config.ts` cannot depend on something tsup has yet to compile.
 */
export declare const components: string[];
export declare const clientEntries: string[];
